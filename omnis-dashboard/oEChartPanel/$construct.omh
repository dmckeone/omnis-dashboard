
Do method buildThemes Returns iThemes

Do $inherited.$init(pId)

If not(isclear(ptTitle))
	Calculate $cinst.$title as ptTitle
End If

If not(isclear(pTheme))
	Calculate $cinst.$theme as pTheme
End If

If not(isclear(pJSONOption))
	Do $cinst.$setJSONOption(pJSONOption)
End If