# Method for rendering this component to a dashboard control
#
# May be overriden if panel substitutions are required for certain circumstances

Do $cinst.$defineRender(data)
For col from 1 to iPropNames.$linecount step 1
	Calculate name as iPropNames.[col].name
	Calculate data.[name] as $cinst.$renderProp(name)
End For

Quit method OJSON.$listorrowtojson(data,kUniTypeCharacter)