# Dispatch the correct panel, based on status

If iError
	Quit method iErrorPanel.$renderJSON()
Else If iLoading
	Quit method iLoadingPanel.$renderJSON()
Else
	Quit method $inherited.$renderJSON()
End If